function personInfo(firstName, lastName, age) {
  let personInfo = {
    firstName: firstName,
    lastName: lastName,
    age: age,
  };
  return personInfo;
}
personInfo("Peter", "Pan", "20");
